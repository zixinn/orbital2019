<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.orbital2019">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.theartofdev.edmodo.cropper.CropImageActivity"/>
        <activity android:name=".intermediate.ShowChatActivity"></activity>
        <activity android:name=".intermediate.message.MessageActivity" />
        <activity android:name=".intermediate.IntermediateActivity" />
        <activity android:name=".intermediate.IntermediateUpdateUserActivity" />
        <activity android:name=".tabs.TabsActivity" />
        <activity android:name=".RegActivity" />
        <activity android:name=".LogActivity" />
        <activity android:name=".intermediate.CreateNewPostActivity" />
        <activity android:name=".intermediate.SellUpdatePostActivity" />
        <activity android:name=".intermediate.BuyViewDetailsActivity" />
        <activity android:name=".intermediate.RateReviewActivity" />
        <activity android:name=".intermediate.FilterSortActivity" />
        <activity android:name=".intermediate.EditProfileActivity" />
        <activity android:name=".intermediate.ViewUsersActivity" />
        <activity android:name=".intermediate.ShowFavouriteActivity" />

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>